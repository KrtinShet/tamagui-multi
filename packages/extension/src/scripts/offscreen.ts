// import {
//   // offscreenSetup,
//   startKeepAlivePolling
// } from "../utils/offscreen";

// startKeepAlivePolling();

// const offscreenBridge = offscreenSetup();

// let timer: any = null;
// Reconnect to the background by reloading the offscreen page
// in case of background downtime or reloading.
// function checkPortEstablished() {
//   if (!offscreenBridge?.portToBg) {
//     clearInterval(timer);
//     window.location.reload();
//   }
// }

// timer = setInterval(checkPortEstablished, 5000);

console.log("offscreen.ts")